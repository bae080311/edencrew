# 학습 원장

뒷다리를 잡은 일을 한 줄로 남긴다. **같은 항목이 두 번 나오면 rule 로 승격**하고, 승격한 곳을 그 줄에 적는다.
고친 그 자리에서 적는다 — 나중에 몰아 쓰면 빠뜨린다.

형식: `YYYY-MM-DD | 증상 → 되먹인 곳`

- 2026-09-11 | **(2회)** `app/` 이 자체 git 저장소인 걸 못 보고 바깥 저장소에 커밋했다. 두 저장소가 remote 까지 같아서 잘못 push 하면 진짜 저장소가 망가질 뻔했다 → 작업 전 `git rev-parse --show-toplevel` 로 루트를 확인한다. 저장소는 루트 하나로 합쳤다(`refactor: 저장소 루트를 작업 폴더로 올리고…`)
- 2026-09-11 | mock 저장을 목표로 오해했다. `NAVER_API.md` 는 mock 을 **권장**으로만 적었고 필수는 요청·파싱·DTO·모델 연결이다 → `rules/data.md` "완료 기준은 실제 연동이다"
- 2026-09-11 | 규칙 한 줄짜리 변경에도 브랜치와 PR 을 따로 팠다 → `rules/conventions.md` 브랜치 3번 "작은 건 진행 중인 브랜치에 얹는다"
- 2026-09-11 | 개발 망(기관망)이 네이버 금융을 차단해 endpoint 4개 중 3개가 TLS 핸드셰이크 직후 끊겼다. 서버 문제로 오해해 헤맸는데 HTTP 로 받아보니 본문이 차단 안내 페이지였다 → **응답이 빈 값일 때 `http://` 로 본문을 확인**하면 망 필터인지 서버 문제인지 바로 갈린다
- 2026-09-11 | 실시간 시세 JSON 도 EUC-KR 이었다. `NAVER_API.md` 는 HTML 만 인코딩을 경고해서 JSON 은 UTF-8 이라 믿고 시작했다 → 응답 `Content-Type` 의 charset 을 endpoint 마다 먼저 확인한다. `rules/data.md` 에 반영
- 2026-09-11 | EUC-KR 디코딩 패키지 고르기에서 두 번 틀렸다. `euc` 는 이름만 보고 골랐는데 EUC-JP 전용이었고, description 까지 확인해 고른 `charset` 은 **`eucKr` 디코딩 자체가 깨져 있었다**(내부 테이블 두 개의 이름이 반대로 붙어 역방향 조회 → `삼성전자` → `鋱鏋飜飅`) → **패키지는 고른 직후 실제 입력 한 줄로 돌려본다.** description 도 증거가 아니다. 유닛 테스트에서 도는지(플랫폼 채널 여부)도 같이 본다. `cp949_codec` 으로 확정
- 2026-09-11 | `finance.naver.com` 이 Dart 기본 User-Agent 로는 표 대신 에러 페이지를 200 으로 돌려줬다. curl(브라우저 UA)로는 10,622바이트인데 Dart UA 로는 2,741바이트 → **응답 크기가 UA 에 따라 다르면 내용까지 확인한다.** `rules/data.md` 에 반영
- 2026-09-11 | mock 을 두 번 받아 장중 시세가 바뀌어 통과했던 테스트가 깨졌다 → **저장한 mock 은 고정 입력이다. 다시 받지 않는다.** `rules/data.md` 에 반영
- 2026-09-11 | `cd .../app` 으로 셸 위치를 옮겨 놓아 사용자가 `app/` 이 따로 remote 에 연결된 것으로 읽었다. 실제로 `.git` 은 루트에만 있었다 → **저장소 루트 혼동이 두 번째다. `rules/conventions.md` Git 로 승격** — git 은 루트에서, `flutter` 는 `(cd app && ...)` 서브셸로.
- 2026-09-12 | 아이콘 경로를 일부러 깨뜨렸는데 `layout_test` 가 그대로 통과했다. `SvgPicture.asset` 은 에셋을 못 찾아도 예외 없이 빈 자리를 그린다 → **에셋 존재는 `rootBundle.load` 로 따로 검증한다**(`icon_asset_test.dart`). 위젯 테스트 통과가 "그려졌다"를 뜻하지 않는다
- 2026-09-12 | 완료된 결과만 캐시해서 **진행 중인 요청이 중복**됐다. 기간 탭을 연달아 누르면 같은 페이지가 두 번 나갔는데, 캐시 히트율만 보던 기존 테스트는 순차 호출이라 이걸 못 잡았다 → 캐시는 **결과가 아니라 future 를 담는다.** 재사용 테스트는 **동시 호출**로도 한 번 짚는다
- 2026-09-12 | 타이밍 버그 회귀 테스트를 처음 썼을 때 **수정 전 코드에서도 통과**했다. 버그 창(입력 변경 ~ 다음 디바운스)이 좁은데 테스트가 그 창을 비껴갔다 → **회귀 테스트는 고치기 전 코드에서 실패하는 걸 먼저 확인한다.** `git stash push <파일>` 로 구현만 되돌려 돌려보면 된다
- 2026-09-12 | 시안에만 있고 Figma 변수에는 없는 간격을 "대응표가 깨진다"는 이유로 위젯 안 `const` 로 뒀다가 리뷰에서 뒤집혔다. `ui.md` 는 **없으면 추가하고 근거를 남기라**고 이미 적어 뒀다 → 토큰 표를 "Figma 변수에서 온 것 / 시안에서 읽은 것" 두 표로 가르면 둘 다 지킬 수 있다. **규칙이 이미 답을 정해 둔 자리에서는 내 절충안을 먼저 의심한다**
- 2026-09-12 | 긴 검색어의 결과 없음 문구를 문구 전체 `maxLines: 2` 말줄임으로 처리했다. 그러면 정작 읽어야 할 `일치하는 검색 결과를 찾지 못했습니다.` 가 통째로 잘려 나간다. `plan/flutter-app.md` 의 판단 항목 표에는 이미 **"20자로 자르고 …"** 라는 초안이 적혀 있었다 → **직접 판단 항목은 구현 전에 그 표를 먼저 읽는다.** 초안이 이미 더 나은 답을 들고 있는 경우가 있다. 말줄임은 **잘려도 되는 쪽**(사용자 입력)에 건다
- 2026-09-12 | 시안 여백(10 · 14)을 "한 위젯의 상자 값"이라며 또 위젯 안 `const` 로 뒀다가 리뷰에서 되돌렸다. **바로 위 줄에 같은 실수가 이미 적혀 있었다** → 스켈레톤 막대처럼 **크기**는 위젯에 남겨도 되지만 **여백은 예외 없이 토큰**이다. `critical.md` 2번이 "간격은 `context.dimens.*` 만"이라 못 박은 자리다. 원장에 적힌 실수를 두 번째로 반복했으니 **작업 시작 전에 `learnings.md` 를 읽는다**
- 2026-09-12 | 동시 호출 회귀 테스트를 이미 갖고 있었는데도 버그가 남았다. `기간을 연달아 바꿔도 진행 중인 페이지를 다시 받지 않는다` 는 **요청 횟수만** 보고 반환값을 안 봤다. 실제로는 진행 중인 1페이지를 건너뛰어 `lastPage` 가 null 인 채로 범위가 계산돼 1년이 10거래일만 받고 있었다 → **캐시·중복 요청 테스트는 "덜 요청했는가" 와 "덜 받았는가" 를 같이 본다.** 요청 수가 줄었다는 건 최적화가 됐다는 뜻도, 결과가 잘렸다는 뜻도 된다
- 2026-09-12 | 상세 화면 여백(앱 바 10, 본문 14, 칩 5, 카드 10·9·3, 표 행 7)을 또 위젯 안 `const` 로 뒀다. **같은 실수가 원장에 이미 두 줄 적혀 있었다** → `learnings.md` 를 "다 쓰고 나서" 읽으니 소용이 없다. **화면 코드를 쓰기 전에 읽는다.** 판별은 간단하다 — `EdgeInsets`·`SizedBox` 로 들어가면 토큰, `width`/`height` 로 들어가면 위젯 상수
- 2026-09-12 | 스크롤 뷰 안 `Column` 의 `Row` 에 `CrossAxisAlignment.stretch` 를 줬다가 `BoxConstraints forces an infinite height` 로 화면이 통째로 안 떴다. 세로가 무한인 곳에서 stretch 는 그 무한을 자식에게 그대로 넘긴다 → 셀 높이를 맞추고 싶으면 `IntrinsicHeight` 거나, 내용 높이가 어차피 같으면 기본값(`center`)으로 둔다. **레이아웃 테스트가 이걸 즉시 잡았다** — 화면을 새로 만들면 테스트부터 돌린다
- 2026-09-12 | 상세 화면 위젯을 한 파일에 쌓다가 `_CurrentPrice` 가 `_DetailViewState._missing` 을, `_Row` 가 `DailyPriceTable._dateWidth` 를 건너 읽고 있었다. 색 매핑은 최상위 함수로 위젯 둘 사이에 끼어 있었다(`WatchlistRow._toneColor` 를 없애고 같은 모양을 다시 만든 셈) → **다른 클래스가 읽는 상수는 파일 최상위 `const` 로**, 색 매핑은 그 색을 쓰는 위젯의 메서드로. 한 파일의 위젯이 셋을 넘으면 나눌 때다
- 2026-09-12 | `?? '-'` 를 View 7곳에 흩어 뒀다. "값이 없을 때 뭘 보여주나" 는 표시 결정이라 ViewModel 이 non-null 로 끝내야 한다 → **View 에 `??` 가 반복되면 그 기본값은 ViewModel 몫이다.** `critical.md` 5번의 경계다
- 2026-09-12 | macOS 앱에서 조회가 전부 실패했는데 `dart run tool/check_endpoints.dart` 는 endpoint 4개를 다 통과시켰다. 점검 도구는 샌드박스 **밖**에서 도니까 `com.apple.security.network.client` 가 빠진 걸 못 잡는다 → **데이터 계층 점검 도구가 통과해도 "앱에서 된다"는 증거가 아니다.** 실행 환경이 다르면 점검도 그 환경에서 한 번은 해야 한다. Flutter macOS 템플릿은 `network.server` 만 넣어주므로 나가는 연결은 직접 켠다
- 2026-09-12 | Lucy 스크립트를 적으면서 `openDialog` 의 인자 순서를 **한국어 API 문서의 항목 나열 순서(알파벳순)** 로 짐작해 `options` 와 콜백을 바꿔 적었다. 실제 시그니처는 앱 번들의 `assets/script/modules/schema.json` 에 `parameters` 배열로 **순서대로** 들어 있었다 → **산문 설명은 인자 순서의 근거가 아니다.** 기계가 읽는 스키마를 먼저 찾는다. 같은 파일에 976개 API 의 타입까지 다 있다
- 2026-09-12 | 행을 "컴포넌트로 분리"하라는 요구를 읽고도, 컴포넌트 안 위젯을 바깥 폼에서 id 로 바로 잡는 스크립트를 적었다. Lucy 는 **노출(readwrite)한 컴포넌트 변수로만** 주고받고 비공개 변수는 무시한다 → **재사용 단위를 만들라는 요구는 곧 경계를 만들라는 뜻이다.** 경계를 넘는 접근을 적기 전에 그 단위의 공개 API가 무엇인지 먼저 본다
- 2026-09-12 | Lucy 스키마의 `methods` 만 보고 `visible`·`current`·`setProperty` 가 **없다고 결론지을 뻔했다.** 프로퍼티는 `setters`/`getters`, 이벤트는 `events` 에 따로 있고, 모든 위젯이 상속하는 공통분은 아예 다른 파일(`objet/base.objet.json`)에 있었다 → **스키마는 한 키만 보고 판단하지 않는다.** 옆에 있는 `manifest.json` 이 어떤 파일이 무슨 역할인지 다 적어 둔다 — 스키마를 열기 전에 매니페스트를 먼저 본다
- 2026-09-12 | 머지된 PR 의 브랜치에 새 커밋을 얹고 푸시했다. `git log origin/main..HEAD` 만 보고 "진행 중인 브랜치"라고 판단했는데 로컬 `origin/main` 이 낡아 있었다 — 원격은 이미 머지 후 브랜치를 지운 상태라 푸시가 `[new branch]` 로 되살렸다 → **브랜치에 얹기 전에 `git fetch --prune` 과 `gh pr view <n> --json state` 로 PR 상태를 먼저 본다.** ref 비교는 fetch 하지 않으면 과거를 본다
- 2026-09-12 | Lucy 가 프로젝트를 못 불러오자 **"토큰 만료"라고 단정**했다. 근거는 두 시간 전에 읽어 둔 `oidc_token_expiry` 였는데 그 사이 앱이 갱신해서 이미 유효했다 → **진단에서 값을 인용할 땐 그 자리에서 다시 읽는다.** 기억 속 값은 과거다. 실제 원인은 앱이 보던 호스트가 NXDOMAIN 인 것이었고, 맞는 주소(`cloud.edencrew.io`)는 **앱 번들 설정이 아니라 공식 문서**에 있었다 — 설치물에 박힌 값이 최신이라는 보장이 없다
- 2026-09-14 | Lucy 등록이 목록에 반영되지 않아 반나절을 태웠다. 원인이 **두 겹**이었는데 하나씩 추측으로 고치느라 계속 헛돌았다 — `closeDialog` 는 다이얼로그를 **연 쪽**에서 부르는 API 라 폼 안에서는 무동작이었고, `$form.setVar` 는 폼에 **선언된 변수**에만 쓰여 저장이 통째로 날아갔다. 돌파구는 `$log.i` 를 네 지점(콜백 도착 · 저장 · 렌더 · 행 쓰기)에 심은 것이었다. `CB saved len=1` 다음 줄이 `RENDER len=0` 인 걸 보고서야 저장이 문제임이 드러났다 → **증상만 보고 두 번 이상 고쳤으면 그때부터는 로그를 먼저 심는다.** 추측 수정 한 바퀴가 로그 한 바퀴보다 비싸다
- 2026-09-14 | 같은 건에서 **문서를 끝까지 안 읽어** 길을 돌았다. `openDialog` 설명에 `options` 가 `width`·`height`·`barrierColor` 를 받는다고 적혀 있었는데, 그걸 못 보고 모달 너비 313 을 레이아웃(SizedBox · crossAxisAlignment · 바깥 패딩)으로 맞추려고 세 번 고쳤다 → **레이아웃으로 씨름하기 전에 그 API 의 옵션부터 읽는다.**
- 2026-09-14 | `errorMessage ?? '기본 문구'` 가 View 세 곳에 또 있었다. **원장에 이미 적힌 항목**("View 에 `??` 가 반복되면 그 기본값은 ViewModel 몫")인데 반복했다 → 규칙을 글로만 두면 또 밟는다. **미사용 토큰 · 반복되는 `??` 처럼 기계로 셀 수 있는 것은 grep 한 줄로 뽑아 본다.** 색 토큰 33개 중 7개가 놀고 있는 것도 같은 방식으로 드러났다
- 2026-09-14 | 그 `??` 를 걷어내려고 getter 를 non-null 로 바꿨더니 **상세 화면의 기간 전환 안내가 항상 뜨는 버그**가 생겼다. 두 실패(첫 조회 · 기간 전환)가 `errorMessage` 하나를 공유하고 있었고 `== null` 로 구분하던 자리였다. `flutter analyze` 의 `info` 한 줄이 잡아 줬다 → **nullable 을 없앨 때는 그 null 이 무엇을 뜻했는지 먼저 찾는다.** null 이 값의 부재가 아니라 상태 구분에 쓰이고 있으면 신호를 나눠야 한다. `info` 수준 경고도 읽는다
- 2026-09-14 | 셸 위치를 `app/lib` 로 옮겨 두는 실수를 **또** 했다(`cd app/lib` 후 다음 명령이 그 위치에서 돌았다). 원장에 이미 두 번 적힌 항목이다 → `(cd app && ...)` 서브셸을 **예외 없이** 쓴다. 탐색용 grep 도 마찬가지다
- 2026-09-14 | 차트 등장 애니메이션이 **전달만 되고 그리기에 안 쓰이는 채로** 통과했다. painter 에 `progress` 를 넘겼는데 포매터가 줄바꿈을 바꿔 놓은 뒤라 문자열 치환이 조용히 실패했고, 내 테스트는 `painter.progress` **값만** 확인해서 초록불이 떴다 → **애니메이션·계산 값의 테스트는 입력이 아니라 결과를 본다.** `PictureRecorder` 로 그려진 양을 0 / 0.5 / 1 에서 비교하니 즉시 드러났다. 그리고 포매터가 손댄 파일에 문자열 치환을 할 때는 성공 여부를 확인한다
- 2026-09-14 | 미사용 토큰(`priceFlatBg`)을 근거로 시안에 없는 UI(등락 배경 배지)를 만들었다. 원문이 "`priceFlatText` / `priceFlatBg`를 사용합니다"라고 적었으니 맞다고 봤는데, Figma 를 직접 여니 세 화면 어디에도 배경이 없었다 → **문서와 시안이 어긋나면 시안이 기준이다.** 원문은 토큰을 준비했다는 뜻일 수 있다. 토큰이 안 쓰인다고 해서 쓸 자리를 지어내지 않는다
- 2026-09-14 | `errorMessage` 의 nullable 을 없애자 상세 화면 기간 전환 안내가 항상 뜨는 버그가 생겼다. 그 null 이 "값 없음"이 아니라 **두 실패를 가르는 상태 구분**에 쓰이고 있었다 → **nullable 을 없애기 전에 그 null 이 무엇을 뜻했는지 먼저 찾는다.** `flutter analyze` 의 `info` 한 줄이 잡아 줬다
- 2026-09-14 | 예외를 삼키면서 **메시지만** 남기고 스택을 버렸다. 같은 문구(`시세를 불러오지 못했습니다`)가 네 경로에서 나와 로그를 봐도 어디서 났는지 몰라 결국 재현했다 → **삼킬 때는 스택도 같이 남긴다.** 그리고 화면이 잡는 건 조회 실패뿐이라 위젯 빌드 오류·`await` 없는 Future 는 아무 데도 안 걸린다 — `FlutterError.onError` 와 `PlatformDispatcher.instance.onError` 를 처음부터 걸어 둔다
- 2026-09-14 | `shared_preferences` 쓰기를 `await` 하지 않아 실패가 통째로 사라지고 있었다. 읽기도 기대한 타입이 아니면 던지는데 `FormatException` 만 잡고 있었다 → **fire-and-forget 은 실패를 버린다는 뜻이다.** 기다리지 않더라도 `catchError` 는 붙인다
- 2026-09-14 | Lucy Studio 0.0.34 — 컴포넌트 인스턴스는 스튜디오 캔버스에만 그려지고 프리뷰 런타임에서는 렌더되지 않는다. 내부 위젯 id 는 밖에서 안 보이고(`txtName is not defined`), 인스턴스의 `setVar`/`getVar` 는 선언한 변수를 모른다(`undefined`). 컴포넌트로 화면 요소를 나누기 전에 **고정 문자열을 박은 인스턴스 하나를 프리뷰에서 먼저 띄워 본다.**
